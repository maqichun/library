/**
	查找指定数据在数组中的索引
*/
function findIndex(msg,array) {
	var index = -1;
	for(var i = 0; i < array.length; i++) {
		if(mas == array[i]) {
			index = i;
		}
	}
	return index;
}
/*卷帘*/
window.onload=function(){
	$("div.fade").addClass("in");
};
